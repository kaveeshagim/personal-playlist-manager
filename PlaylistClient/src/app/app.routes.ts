import { Routes } from '@angular/router';
import { PlaylistListComponent } from './components/playlist-list/playlist-list.component';

export const routes: Routes = [{ path: '', component: PlaylistListComponent }];
